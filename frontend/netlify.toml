[build]
  publish = "dist"
  command = "npm run build"

[build.environment]
  # Set production API base for Netlify builds.
  VITE_API_BASE = "https://ai-startup-validator-chi.vercel.app"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
